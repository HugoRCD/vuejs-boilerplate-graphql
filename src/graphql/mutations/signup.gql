mutation createUser($user: CreateUserInput!) {
    authSignup(user: $user) {
        token
        user {
            email
            username
            firstname
            lastname
            role
            isVerified
        }
    }
}