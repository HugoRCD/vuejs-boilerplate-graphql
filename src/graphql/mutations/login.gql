mutation login($username: String!, $password: String!) {
    authLogin(username: $username, password: $password) {
        token
        user {
            id
            username
            firstname
            lastname
            email
            role
            isVerified
        }
    }
}