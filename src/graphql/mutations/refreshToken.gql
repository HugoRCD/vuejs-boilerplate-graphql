mutation refreshToken ($refreshToken: String!) {
    RefreshToken(refreshToken: $refreshToken) {
        accessToken
        refreshToken
        user {
            id
            username
            email
            isVerified
        }
    }
}